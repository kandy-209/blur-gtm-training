name: Daily Dependency Check

on:
  schedule:
    # Run daily at 2 AM UTC (adjust timezone as needed)
    - cron: '0 2 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  check-dependencies:
    name: Check for Updates
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Check for outdated packages
        id: outdated
        run: |
          echo "📦 Checking outdated packages..."
          OUTDATED=$(npm outdated --json || echo "{}")
          echo "outdated=$OUTDATED" >> $GITHUB_OUTPUT
          
          if [ "$OUTDATED" != "{}" ]; then
            echo "⚠️ Found outdated packages"
            echo "$OUTDATED" | jq -r 'to_entries[] | "  • \(.key): \(.value.current) → \(.value.latest)"'
            echo "has_updates=true" >> $GITHUB_OUTPUT
          else
            echo "✅ All packages are up to date!"
            echo "has_updates=false" >> $GITHUB_OUTPUT
          fi
      
      - name: Check for security vulnerabilities
        id: audit
        run: |
          echo "🔒 Checking for security vulnerabilities..."
          AUDIT=$(npm audit --json || echo "{}")
          echo "audit=$AUDIT" >> $GITHUB_OUTPUT
          
          VULN_COUNT=$(echo "$AUDIT" | jq '.vulnerabilities | length' || echo "0")
          
          if [ "$VULN_COUNT" -gt 0 ]; then
            CRITICAL=$(echo "$AUDIT" | jq '[.vulnerabilities[] | select(.severity == "critical")] | length' || echo "0")
            HIGH=$(echo "$AUDIT" | jq '[.vulnerabilities[] | select(.severity == "high")] | length' || echo "0")
            MODERATE=$(echo "$AUDIT" | jq '[.vulnerabilities[] | select(.severity == "moderate")] | length' || echo "0")
            LOW=$(echo "$AUDIT" | jq '[.vulnerabilities[] | select(.severity == "low")] | length' || echo "0")
            
            echo "⚠️ Found $VULN_COUNT vulnerabilities:"
            echo "  Critical: $CRITICAL"
            echo "  High: $HIGH"
            echo "  Moderate: $MODERATE"
            echo "  Low: $LOW"
            echo "has_vulnerabilities=true" >> $GITHUB_OUTPUT
          else
            echo "✅ No security vulnerabilities found!"
            echo "has_vulnerabilities=false" >> $GITHUB_OUTPUT
          fi
      
      - name: Create summary report
        run: |
          echo "# 📊 Daily Dependency Check Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Date:** $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.outdated.outputs.has_updates }}" == "true" ]; then
            echo "⚠️ **Outdated Packages Found**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "${{ steps.outdated.outputs.outdated }}" | jq -r 'to_entries[] | "- **\(.key)**: \(.value.current) → \(.value.latest)"' >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ **All packages are up to date**" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.audit.outputs.has_vulnerabilities }}" == "true" ]; then
            echo "🔒 **Security Vulnerabilities Found**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Run \`npm audit fix\` to automatically fix vulnerabilities." >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ **No security vulnerabilities found**" >> $GITHUB_STEP_SUMMARY
          fi
      
      - name: Comment on PR (if applicable)
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const outdated = '${{ steps.outdated.outputs.has_updates }}' === 'true';
            const vulnerabilities = '${{ steps.audit.outputs.has_vulnerabilities }}' === 'true';
            
            if (outdated || vulnerabilities) {
              const body = `## 🔍 Dependency Check Results
              
              ${outdated ? '⚠️ **Outdated packages found** - Consider updating dependencies' : '✅ All packages up to date'}
              ${vulnerabilities ? '🔒 **Security vulnerabilities found** - Run `npm audit fix`' : '✅ No security vulnerabilities'}
              
              Check the workflow logs for details.`;
              
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: body
              });
            }

